[project]
name = "hidipi"
version = "0.2.1"
description = "Local macOS HiDPI controls with verified backups and automatic rollback"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
hidipi = "hidipi.cli:main"
hidpi = "hidipi.cli:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
